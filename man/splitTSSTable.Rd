% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/TSStableTools.R
\name{splitTSSTable}
\alias{splitTSSTable}
\title{Extract samples from a raw-count TSS table}
\usage{
splitTSSTable(
  inputFile,
  samples,
  outputFile = NULL,
  dropZero = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{inputFile}{Path to one TSS table.}

\item{samples}{Character vector of sample columns to extract.}

\item{outputFile}{Optional path for the extracted table. If \code{NULL}, no
file is written.}

\item{dropZero}{Logical indicating whether rows containing zero for every
selected sample should be removed.}

\item{overwrite}{Logical indicating whether an existing output file may be
replaced.}
}
\value{
A base \code{data.frame} containing coordinates and the selected
  samples.
}
\description{
Select sample columns from a TSS table and optionally remove
  positions at which every selected sample has a zero count.
}
\details{
This function selects columns; it does not rename or sum samples.
  With \code{dropZero = TRUE}, a row is retained when at least one selected
  sample has a non-zero raw count. Input validation and metadata handling are
  the same as for \code{combineTSSTables()}.
}
\examples{
fixture <- system.file(
    "extdata", "example-tss-table.tsv", package = "TSSr"
)
control <- splitTSSTable(fixture, samples = c("SL01", "SL02"))
head(control)
}
